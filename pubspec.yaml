name: unified_popups
description: A unified popups SDK for Flutter, providing a flexible way to show toasts, dialogs, and other custom popups.
version: 1.0.0
homepage: # TODO: Add your repository URL here

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  uuid: ^4.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0


